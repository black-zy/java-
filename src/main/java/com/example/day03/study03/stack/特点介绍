1  栈的特点：
     栈 --线程运行需要的内存空间     -Xss   指定栈内存的大小

     栈的特点：先进后出

     组成：由栈帧组成    每个方法运行时需要的内存

     	  参数   局部变量  返回数据

   栈内存溢出问题
       栈帧过多导致栈内存溢出
       栈帧过大导致栈内存溢出


   常规检测工具：jstack   线程编号（可以定位到代码）